import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:bioclient/app/modules/home/driver_store.dart';
import 'package:bioclient/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // DriverStore driver;
  //
  setUp(() {
    //     driver = HomeModule.to.get<DriverStore>();
  });

  group('DriverStore Test', () {
    //   test("First Test", () {
    //     expect(driver, isInstanceOf<DriverStore>());
    //   });

    //   test("Set Value", () {
    //     expect(driver.value, equals(0));
    //     driver.increment();
    //     expect(driver.value, equals(1));
    //   });
  });
}
